# GIT_AUTHOR_NAME="0xADADA"
# GIT_COMMITTER_NAME="$GIT_AUTHOR_NAME"
# git config --global user.name "$GIT_AUTHOR_NAME"
# GIT_AUTHOR_EMAIL="CHANGEME@example.com"
# GIT_COMMITTER_EMAIL="$GIT_AUTHOR_EMAIL"
# git config --global user.email "$GIT_AUTHOR_EMAIL"
# git config --global user.signingkey 6BBE9BB119656CA4
#
# # ibus settings (IME tools)
# GTK_IM_MODULE=ibus
# QT_IM_MODULE=ibus
# XMODIFIERS="@im=ibus"  # older x-org applications
#
# # add XCode to the PATH
# PATH="${PATH}:$(xcode-select --print-path)/usr/bin"
#
# # add yarn to PATH
# PATH="${HOME}/.config/yarn/global/node_modules/.bin:${PATH}"
#
# # add ruby gems to PATH
# export GEM_PATH="${HOME}/.gem"
# PATH="${GEM_PATH}/bin:${PATH}"
#
# # Setup asdf
# source /usr/local/opt/asdf/asdf.sh
# # Setup rvm
# source "${HOME}/.rvm/scripts/rvm"
#
# alias aws-update-certs='test -e ~/.aws/credentials && mv ~/.aws/credentials ~/.aws/credentials.bak.\`date +%Y%b%d-%H%M%S\`; mv ~/Desktop/credentials ~/.aws/credentials && echo -e "\nregion = us-east-1" >> ~/.aws/credentials'
